/*
  Проверку на пустой список можно осуществлять с помощью свойства length.
  Если подразумевается дополнительная логика проверки, то я бы оставил функцию isEmpty.
*/

const list = []

if (list.length !== 0) {
  // код, если список не пуст
}
